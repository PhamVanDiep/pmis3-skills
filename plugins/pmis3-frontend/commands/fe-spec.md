---
name: "FE Spec Implement"
description: Implement full CRUD feature (model + service + page + route) từ file SPEC backend mô tả
category: Development
tags: [feature, crud, spec, angular]
---

Implement FE feature từ file SPEC.

**SPEC file path**: `$ARGUMENTS`

Invoke the `fe-spec-implement` skill to implement the feature described in the SPEC file above.

If `$ARGUMENTS` is empty, ask the user for the SPEC file path (e.g., `docs/FE_CA_TRUC_SPEC.md`).
